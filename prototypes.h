#ifndef PROTOTYPES
#define PROTOTYPES

#include "structs.h"

void myInit(void);
void defineBaseHouse(struct base*);
void drawBaseHouse(struct base*);
void reshape(int , int );
void display(void);

#endif

